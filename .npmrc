# 1. 核心源：淘宝镜像
registry=https://registry.npmmirror.com/

# 2. 常见二进制包镜像加速 (解决 node-sass, electron 等安装卡死问题)
sass_binary_site=https://npmmirror.com/mirrors/node-sass/
phantomjs_cdnurl=https://npmmirror.com/mirrors/phantomjs/
electron_mirror=https://npmmirror.com/mirrors/electron/
sqlite3_binary_host_mirror=https://npmmirror.com/mirrors/sqlite3/
profiler_binary_host_mirror=https://npmmirror.com/mirrors/node-inspector/
chromedriver_cdnurl=https://npmmirror.com/mirrors/chromedriver/

# 3. 严格依赖处理 (可选，推荐团队协作开启)
# 作用：安装时若 lock 文件与 package.json 不一致，报错而不是悄悄修改 lock 文件
# engine-strict=true

# 之前的配置保持不变...

# Electron Builder 专用二进制源 (解决 winCodeSign, nsis 下载失败)
electron-builder-binaries-mirror=https://registry.npmmirror.com/-/binary/electron-builder-binaries/